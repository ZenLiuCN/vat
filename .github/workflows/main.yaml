name: deploy package to the Maven Central Repository
on:
  push:
    tags:
      - '\d*' # Push events to matching v*, i.e. v1.0, v20.15.10
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v5
        with:
          cache: 'maven'
          java-version: '21'
          distribution: 'liberica'
          server-id: central
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
          gpg-private-key: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }} # Value of the GPG private key to import
          gpg-passphrase: MAVEN_GPG_PASSPHRASE # env variable for GPG private key passphrase
      - name: release
        run: |
          mvn versions:set -DnewVersion=$GITHUB_REF_NAME 
          mvn -B package -Dvat.versions=$GITHUB_REF_NAME -DskipTests -Pcentral --file pom.xml --no-transfer-progress && mvn deploy --no-transfer-progress
        env:
          MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.OSSRH_TOKEN }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
